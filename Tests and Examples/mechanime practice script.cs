using UnityEngine;
using System.Collections;

public class mechanimepracticescript : MonoBehaviour {
	/*
	// Use this for initialization
	void Start () {
		anim = GetComponent<Animator>();
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetAxis("Horizontal") > 0) { //going right
			anim.SetInt("direciton", 2);
		}
		
		else if(Input.GetAxis ("Horizontal") = 0) { //idling
			anim.SetInt("direction", 0);
		}
		
		else if(Input.GetAxis("Horizontal") < 0) { //going left
			anim.SetInt("direciton", 1);
		}
	}
	*/
}
